import React from 'react';
import ScreenPlaceholder from '@/components/ScreenPlaceholder';

export default function Workout() {
  return (
    <ScreenPlaceholder
      icon="fitness_center"
      title="Séance"
      subtitle="Vue d'ensemble de ta séance, exercices, séries et chrono de repos arrivent ici."
    />
  );
}
