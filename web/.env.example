# Configuration de l'app web GymAI (Vite).
# Copie ce fichier en `.env` (ou `.env.local`) et adapte les valeurs.

# Base de l'API backend GymAI.
# Défaut si non défini : http://localhost:8000/api/v1
VITE_API_URL=http://localhost:8000/api/v1
